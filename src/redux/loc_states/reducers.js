const initialState = {
  items: [],
  loading: true,
  loaded: false,
}

export default function statesReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
