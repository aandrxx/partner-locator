const initialState = {
  items: [],
  loading: false,
  loaded: false,
}

export default function statesReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
